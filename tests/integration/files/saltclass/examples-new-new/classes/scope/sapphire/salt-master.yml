classes:
  - role.salt-master
  - .   #  attach init from the same level if present - class scope.sapphire defined by classes/scope/sapphire/init.yml

pillars:
  salt:
    master:
      conf:
        # TODO: add full example after implementation
        master_tops:
          saltclass:
            path: /var/salt/saltclass
        ext_pillar:
          - ^  # guarantee this is the only extpillar we use
          - saltclass: 
            - path: /var/salt/saltclass
